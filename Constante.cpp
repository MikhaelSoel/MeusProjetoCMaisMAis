#include <iostream>
int main ()
{
    const[tipo][nome] = [valor];
    const NUM_VIDAS = 10; //cria uma região na memirua
    const int BONUS = 200; //Cria uma constante inteira que armazena 200
    const double PI = 3.1415; //Uma constante ponto flutuante de dupla precisão que armazena o valor aproximado de pi
    #define NUM_VIDAS 10; // PODE ser usado para criar constantes mas existem difernças, é uma diretiva de pre processador de codigo. Não cria nada na meemoria
}